package mops.gruppen1.domain;

import java.util.UUID;

/**
 * Representation of a user within the domain.
 */
public class User {
    private UUID userId; // umbenennen in Login name?
    private Username username; // umbenennen in Klarnamen?
}
